# Media model
# TODO: Implement Media model with fields: id, user_id, filename, mime_type, original_blob_path, preview_blob_path,
# TODO: upload_date, width, height, duration (for videos), embedding (vector), ai_tags, ai_processed, created_at
